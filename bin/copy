#!/bin/sh
#
# Copy to clipboard

if test "$(uname)" = "Darwin"; then
	pbcopy
else
  if (( $+commands[xclip] )); then
    xclip -in -selection clipboard
  else
    echo "clipcopy: Platform $OSTYPE not supported or xclip not installed"
    return 1
  fi
fi
